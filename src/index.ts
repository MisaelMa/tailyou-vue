import {} from "./components";

import type { App, version } from "vue";

export {
  Button,
  Toolbar,
  TableCustom,
  Card,
  Pagination,
  TableModal,
  Select,
  Sidebar,
  IFile,
  Carousels,
} from "./components";

/* export default {
  install: (app: App) => {
    app.component("Button", Button);
  },
};
 */
